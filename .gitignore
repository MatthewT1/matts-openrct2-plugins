# Dependencies and build output (release builds go to GitHub Releases / CI artifacts)
node_modules/
dist/
tests/build/

# Local reference clone of the OpenRCT2 source (~400 MB, has its own .git)
gamesrc/

# Private session notes and telemetry summaries
notes/

# Debug channel logs (can be tens of MB)
tools/rct-debug.log*

# Editor / OS / agent cruft
.vscode/
.idea/
.claude/
Thumbs.db
.DS_Store
*.log

# Headless harness output (#46): per-run user-data copies, logs, CSVs
harness-runs/

# Machine-specific harness pool folders (#72)
tools/headless/pool.local.json
